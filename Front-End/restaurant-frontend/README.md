# Restaurant Frontend

React + Vite frontend for the restaurant final project. It provides the public restaurant pages, authentication modals, profile updates, booking flow, menu browsing, cart UI, and the admin dashboard for users, menu items, and bookings.

Run locally with `npm run dev` and lint with `npm run lint`.
