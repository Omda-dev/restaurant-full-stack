# Restaurant API

Laravel API for the restaurant final project. It handles authentication with Sanctum tokens, users, menu categories/subcategories, menu items, profile images, and table bookings.

Run locally with `php artisan serve`, migrate with `php artisan migrate --seed`, and test with `php artisan test`.
